#Gothic

#自动生成项目目录结构，以及demo
#执行以下命令
1. cd $GOPATH
2. go get github.com/gothinc/gothic
3. cd github.com/gothinc/gothic/tools
4. sh generate.sh -b $GOPATH/yourappname
5. cd yourappname
6. ./make
7. ./serverctl -s start

访问http://127.0.0.1:9919/demo/getMsg?name=gothic

=============================================================================================

#wiki

#启动服务
import github.com/gothinc/gothic

gothic.Application.AddController(&controller.YourController{})
gothic.Application.Run()

注：
1. controller以Controller结尾，如DemoController，行为以Action结尾, 如GetScoreAction
   则url为/demo/getScore


#模板配置
[application]
name = "gothic"
server_header = "GothicServer"
version = 1.0

[log]
root = "/logroot/yourpath"
prefix = "gothic-"
suffix = ".log"
json_format = true
level = 1 #debug(0), access(1), warn(2), error(3)

[server]
http_port = 9918
http_read_timeout = 2000  #ms
http_write_timeout = 2000  #ms
http_idle_timeout = 60000  #s
